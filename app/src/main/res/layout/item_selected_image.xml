<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_selected_image_root_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="4dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp">

    <ImageView
        android:id="@+id/item_selected_image_image_view"
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintDimensionRatio="1:1"
        tools:src="@tools:sample/avatars"/>

    <ImageView
        android:id="@+id/item_image_index_text_view"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:background="@drawable/background_selected_image_delete"
        android:gravity="center"
        app:layout_constraintBottom_toTopOf="@id/item_selected_image_image_view"
        app:layout_constraintEnd_toEndOf="@id/item_selected_image_image_view"
        app:layout_constraintStart_toEndOf="@id/item_selected_image_image_view"
        app:layout_constraintTop_toTopOf="@id/item_selected_image_image_view"/>
</androidx.constraintlayout.widget.ConstraintLayout>